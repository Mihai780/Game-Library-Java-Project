meta {
  name: Create Mario User
  type: http
  seq: 21
}

post {
  url: {{baseUrl}}/rest/users
  body: json
  auth: inherit
}

body:json {
  { "username": "alex", "balanceCents": 0 }
}

settings {
  encodeUrl: true
  timeout: 0
}
