meta {
  name: Create Survival Tag
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/rest/tags
  body: json
  auth: inherit
}

body:json {
  { 
    "name": "Survival" 
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
