meta {
  name: Create Mihai User
  type: http
  seq: 18
}

post {
  url: {{baseUrl}}/rest/users
  body: json
  auth: inherit
}

body:json {
  { "username": "mihai", "balanceCents": 0 }
}

settings {
  encodeUrl: true
  timeout: 0
}
