meta {
  name: Create Alex User
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/rest/users
  body: json
  auth: inherit
}

body:json {
  { "username": "mihai", "balanceCents": 0 }
}

settings {
  encodeUrl: true
  timeout: 0
}
