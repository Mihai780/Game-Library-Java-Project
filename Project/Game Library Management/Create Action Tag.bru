meta {
  name: Create Action Tag
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/rest/tags
  body: json
  auth: inherit
}

body:json {
  { 
    "name": "Action" 
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
