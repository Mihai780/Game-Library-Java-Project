meta {
  name: Create Strategy Tag
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/rest/tags
  body: json
  auth: inherit
}

body:json {
  { 
    "name": "Strategy" 
  }
  
}

settings {
  encodeUrl: true
  timeout: 0
}
